#include <iostream>
#include <vector>

int main()
{
    long long i;
    std::cin >> i;

    // get memory from the stack
    // this memory is WITHIN the bounds of the program itself.
    int arr[10] = {0};

    // how to allocate RAM/heap

}